apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: max-effort-nginx
spec:
  type: {{ .Values.service.type }}
  selector:
    app: max-effort-nginx
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
