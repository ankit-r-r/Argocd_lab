apiVersion: apps/v1
kind: Deployment
metadata:
  name: max-effort-nginx
  labels:
    app: max-effort-nginx
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: max-effort-nginx
  template:
    metadata:
      labels:
        app: max-effort-nginx
    spec:
      containers:
        - name: max-effort-nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
