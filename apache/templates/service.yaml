apiVersion: v1
kind: Service
metadata:
  name: apache-service-max-effort
spec:
  type: {{ .Values.service.type }}

  selector:
    app: max-effort-apache

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
