apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: max-effort-grafana-pvc
  labels:
    app: max-effort-grafana
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  storageClassName: {{ .Values.persistence.storageClass }}
